package com.meuprojeto.banco.sistemabancario.controller.dto.account;

//ideia de retornar para o Gerente dados não sensiveis da tabela de clientes
public record AccountDTOManagerResponse(
        //id cliente, nome do cliente, tipo de conta, data_criação, ativa
) {
}
