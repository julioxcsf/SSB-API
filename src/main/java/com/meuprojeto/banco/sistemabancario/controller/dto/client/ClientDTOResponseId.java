package com.meuprojeto.banco.sistemabancario.controller.dto.client;

import java.util.UUID;

//usado para retornar o id quando buscado com o email do token
public record ClientDTOResponseId(UUID id) {
}
