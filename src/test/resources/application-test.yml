spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: jtest
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true

jwt:
  secret: teste12345678901234567890123456789012
  expiration: 3600000  # 1 hora em milissegundos, se usar

#desativa segurança
#security:
#  enable: false